<%= link_to image_tag("flowery-logo.png", :class => "center"), "http://#{@city.domain}.#{Rails.configuration.hosts[Rails.env]}"%>

<div class="monthly-workshops">
  <ul class="list-group">
    <a class="list-group-item">
      <h4 class="list-group-item-heading"> <%=t(:this_month) %> </h4>
    </a>
    <% @this_month_workshops.each do |id, name| %>
      <li class="list-group-item"> <%= link_to name, workshop_path(id)%> </li>
    <% end %>
    </ul>
  </div>
  <% if (@next_month_workshops.count > 0 ) %>
    <h5><%=t(:next_month) %></h5>
    <ul class="nav nav-list">
      <% @next_month_workshops.each do |id, name| %>
        <li> <%= link_to name, workshop_path(id)%> </li>
      <% end %>
    </ul>
  <% end %>

  <div>
    <script>
      (function() {
        var cx = '009592581567718586061:yqewdu7b6vs';
        var gcse = document.createElement('script');
        gcse.type = 'text/javascript';
        gcse.async = true;
        gcse.src = (document.location.protocol == 'https:' ? 'https:' : 'http:') +
        '//www.google.com/cse/cse.js?cx=' + cx;
      var s = document.getElementsByTagName('script')[0];
      s.parentNode.insertBefore(gcse, s);
      })();
    </script>
    <gcse:search></gcse:search> 
  </div>
    <div clas="row">
      <div class="col-md-4 col-md-offset-2">
        <%= link_to image_tag("facebook.png", :class => "center"), "https://facebook.com/#{@city.facebook_page_id}" unless @city.facebook_page_id.empty? %>
      </div>
      <div class="col-md-4">
        <%= link_to image_tag("paperplane.png", :class => "center"), "http://#{@city.domain}.#{Rails.configuration.hosts[Rails.env]}"%>
      </div>
  </div>
