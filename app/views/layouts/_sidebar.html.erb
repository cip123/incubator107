<%= link_to image_tag("flowery-logo.png", :class => "center"), "http://#{@city.domain}.#{Rails.configuration.hosts[Rails.env]}"%>

<% if @this_month_workshops.count > 0 %>
<div class="monthly-workshops">
  <ul class="list-group">
    <a class="list-group-item">
      <h4 class="list-group-item-heading"> <%=t(:this_month) %> </h4>
    </a>
<% @this_month_workshops.each do |id, name| %>
    <li class="list-group-item"> <%= link_to name, workshop_path(id)%> </li>
<% end %>
  </ul>
</div>

<br/>
<br/>
<% end %>

<% if @next_month_workshops.count > 0 %>

<div class="monthly-workshops">
  <ul class="list-group">
    <a class="list-group-item">
      <h4 class="list-group-item-heading"> <%=t(:next_month) %> </h4>
    </a>
    <% @next_month_workshops.each do |id, name| %>
    <li class="list-group-item"> <%= link_to name, workshop_path(id)%> </li>
    <% end %>
  </ul>
</div>
<% end %>
  <div>
    <script>
      (function() {
        var cx = '009592581567718586061:yqewdu7b6vs';
        var gcse = document.createElement('script');
        gcse.type = 'text/javascript';
        gcse.async = true;
        gcse.src = (document.location.protocol == 'https:' ? 'https:' : 'http:') +
        '//www.google.com/cse/cse.js?cx=' + cx;
      var s = document.getElementsByTagName('script')[0];
      s.parentNode.insertBefore(gcse, s);
      })();
    </script>
    <gcse:search></gcse:search> 
  </div>
    <div clas="row">
      <div class="col-md-4 col-md-offset-2">
        <%= link_to image_tag("facebook.png", :class => "center"), "https://facebook.com/#{@city.facebook_page_id}", :target => "_blank" unless @city.facebook_page_id.empty? %>
      </div>
      <div class="col-md-4 newsletter-subscriber">
          <a id="subscribe-newsletter" data-placement="auto" href="#" title="<%=t(:register_to_newsletter) %>" data-toggle="popover" data-html="true" data-content='
          <%= form_for( @subscriber, url: {controller: "/subscriber", action: "register_newsletter"}, remote: true) do |f| %>
          <%= f.text_field :email,  autofocus: true, placeholder: "Email" %>
          <% end %>'>
            <%= image_tag("paperplane.png", :class => "center") %>
          </a>
      </div>
  </div>
  
